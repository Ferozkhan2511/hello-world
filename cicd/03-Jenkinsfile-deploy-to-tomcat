pipeline {
  agent any
  tools {
    maven 'maven-3.6.3' 
  }
  stages {
    stage ('Build') {
      steps {
        bat 'mvn clean package'
      }
    }
    stage ('Deploy') {
      steps {
        script {
          deploy adapters: [tomcat8(credentialsId: '1380f6f8-60b7-4069-a4c8-c93d35bad5c5', path: '', url: 'http://localhost:9090/ ')], contextPath: '/pipeline, onFailure: false, war: 'webapp/target/hello-world.war'  
        }
      }
    }
  }
}
